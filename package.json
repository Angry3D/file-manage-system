{
  "name": "file-manage-system",
  "version": "0.1.0",
  "private": true,
  "description": "Full-stack file management system workspace.",
  "engines": {
    "pnpm": ">=10 <11"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm -r --if-present run test",
    "verify": "pnpm run verify:lock && pnpm run verify:server && pnpm run verify:web-admin && pnpm run verify:web-h5",
    "verify:lock": "pnpm install --lockfile-only --frozen-lockfile",
    "verify:server": "node --check server/src/config/env.js && node --check server/src/config/adapter.js && node --check server/src/config/define.js && node --check server/src/config/config.js && node --check server/src/config/config.production.js && node --check server/src/controller/admin/image.js && node --check server/src/controller/common/upload.js && sh -n server/script/file-server.sh",
    "verify:web-admin": "pnpm --filter ./web-admin build",
    "verify:web-h5": "pnpm --filter ./web-h5 build",
    "audit:prod": "pnpm audit --prod"
  },
  "pnpm": {
    "overrides": {
      "think-validator>validator": "13.15.22",
      "think-ms>ms": "2.1.3",
      "think-helper>ms": "2.1.3",
      "think-helper>uuid": "11.1.1"
    }
  }
}
