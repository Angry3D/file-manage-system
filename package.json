{
  "name": "file-manage-system",
  "version": "0.1.0",
  "private": true,
  "description": "Full-stack file management system workspace.",
  "engines": {
    "pnpm": ">=10 <11"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm -r --if-present run test"
  }
}
